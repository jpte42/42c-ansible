---
# Per-interface hostname.if content, keyed by interface name. Define per
# host in host_vars/<host>/main.yml, e.g.:
#   router_interfaces:
#     em0: |
#       inet 203.0.113.1/24
router_interfaces: {}
